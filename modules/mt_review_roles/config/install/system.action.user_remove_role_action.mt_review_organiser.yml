langcode: en
status: true
dependencies:
  config:
    - user.role.mt_review_organiser
  module:
    - user
id: user_remove_role_action.mt_review_organiser
label: 'Remove the Review Organiser role from the selected user(s)'
type: user
plugin: user_remove_role_action
configuration:
  rid: mt_review_organiser
