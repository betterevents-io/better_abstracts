langcode: en
status: true
dependencies:
  config:
    - flag.flag.mt_abstract_accepted
  module:
    - flag
id: flag_action.mt_abstract_accepted_flag
label: 'Mark this abstract as accepted'
type: node
plugin: 'flag_action:mt_abstract_accepted_flag'
configuration:
  flag_id: mt_abstract_accepted
  flag_action: flag
