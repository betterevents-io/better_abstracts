langcode: en
status: true
dependencies:
  config:
    - user.role.mt_observer
  module:
    - user
id: user_add_role_action.mt_observer
label: 'Add the Observer role to the selected user(s)'
type: user
plugin: user_add_role_action
configuration:
  rid: mt_observer
