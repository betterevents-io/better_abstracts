langcode: en
status: true
dependencies:
  config:
    - user.role.mt_reviewer
  module:
    - user
id: user_remove_role_action.mt_reviewer
label: 'Remove the reviewer role from the selected user(s)'
type: user
plugin: user_remove_role_action
configuration:
  rid: mt_reviewer
