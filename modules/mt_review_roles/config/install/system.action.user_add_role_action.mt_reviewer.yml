langcode: en
status: true
dependencies:
  config:
    - user.role.mt_reviewer
  module:
    - user
id: user_add_role_action.mt_reviewer
label: 'Add the reviewer role to the selected user(s)'
type: user
plugin: user_add_role_action
configuration:
  rid: mt_reviewer
